name: Windows Build Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: windows-latest

    steps:
      - name: Sourcecode laden
        uses: actions/checkout@v4

      - name: Test
        run: echo "KPLive Ads Keyer Build funktioniert"
